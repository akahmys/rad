# PLANS

## 目的
`rad` (Rust Agent Dispatcher) プロジェクトのアーキテクチャ仕様書および基盤ドキュメントを作成・管理する。

## 計画
1. `README.md` の作成（完了）
2. ユーザーフィードバックに基づく `README.md` のリファイン（完了）
3. 導入文の調整と全体の簡素化（完了）
4. 導入文の表現修正（完了）
5. 「設定とエクステンション管理」節の削除（完了）
6. `rad` アーキテクチャ設計書 (`ARCHITECTURE.md`) の作成 (完了)
7. リモートリポジトリ設定・バージョン0（`v0.0.0`）タグ作成・GitHubへのプッシュ (保留)
8. 設定ファイル名を `rad.json` に統一し、配置ルールを反映した `CONFIG.md` の改定および `ARCHITECTURE.md` の修正 (完了)
9. 推奨案に基づくグローバルな配置ルールを `CONFIG.md` に追記・アップデート (完了)
10. 最低限の `rad.json` スキーマの検討と `CONFIG.md` の改定、および `rad.json` ファイルの新規作成 (完了)

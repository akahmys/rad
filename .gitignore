/target
# Nested build output — `templates/*` are standalone crates with their own.
**/target/
.rad/

# macOS
.DS_Store

# Local tooling artifacts (databases, indexes)
.cocoindex_code/

# Per-machine Claude Code settings — contains absolute paths by nature.
.claude/settings.local.json

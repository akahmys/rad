[package]
name = "openai-orchestrator"
version = "0.6.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
serde_bytes = "0.11"
rad-models = { path = "../../models" }

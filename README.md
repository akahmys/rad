# rad (Rust Agent Dispatcher)

`rad` is a minimal, ultra-fast agent-oriented shell runtime designed with a strict separation between intelligence (policy) and execution (mechanism). It provides event dispatching and robust OS process supervision.

`rad` is a coding agent runtime inspired by the minimalist philosophy of `pi-coding-agent` (https://pi.dev/).

---

## 1. Features

*   **Single Static Binary & Zero Dependencies**: Lightweight Rust design. Starts instantly with a tiny memory footprint of just a few megabytes.
*   **OS-level Process Cleanup (PGID Management)**: Ensures all external processes generated by the agent (including grandchildren and descendants) are forcefully terminated via PGID (`SIGKILL`) when the core exits.
*   **Fast Rollback Without Polluting Git**: Automatically backs up file changes to `.rad/snapshots/`. Instantly restores the filesystem to any checkpoint without touching Git history.
*   **Wasm-based Plugins & Gateway Access Control**: Executes agent policy logic inside a secure WebAssembly sandbox (Wasmer/Wasmtime). All physical operations (I/O, process spawning) are routed through a single API Gateway in the Core, which physically blocks unauthorized actions using capability masks (`rad.json`).
*   **Stateless Policy & DAG Context**: Extensions are designed to be stateless, reloading history dynamically from Core's DAG (Directed Acyclic Graph) representation to ensure resiliency across restarts.


---

## 2. Quick Start

### 2.1 Prerequisites

*   Rust 1.75 or higher
*   `cargo`

### 2.2 Build Instructions

```bash
# Clone and build
git clone https://github.com/yourusername/rad.git
cd rad
cargo build --release
```

### 2.3 Running Tests

```bash
cargo test
```

---

## 3. License

[MIT License](LICENSE)
